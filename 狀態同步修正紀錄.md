# 狀態同步修正紀錄

日期：2026-09-10。

## 原因與調查界線

確認存在的儲存缺陷：原本「儲存本項確認」按鈕只顯示成功訊息，並沒有提交結果及備註。資料寫入依賴 selectbox、text_area 的 on_change。所有項目共用 decision_input、note_input，且每次 rerun 都由案件資料覆寫 widget 值，使「按下儲存」沒有可靠的一次性提交語意。舊測試分別觸發各欄位回呼，沒有測試真正的表單提交。

分析畫面的限制：舊版以 st.empty 的子容器在同一次執行中反覆替換進度與五張卡片，另有本地 active 參數以及 stage、analyzed 兩個案件欄位。AppTest 未重現使用者看到的前端殘留卡片，因此不能聲稱已證實瀏覽器殘留的唯一原因；本次移除這個局部更新結構及重複完成旗標，直接消除兩套呈現來源。

未發現每次 rerun 都重新初始化案件，也沒有另一組歷史頁專用 reviews；問題集中在提交來源及更新方式，而非跨頁建立第二份案件。

## 單一狀態來源

- `session_state.case.reviews[規則編號]` 是唯一已儲存的處理結果與備註來源，保留 decision、note。非「尚未確認」的已儲存結果即為已檢視，不另存容易漂移的 reviewed 布林值。
- 每項表單採獨立鍵 `review_decision_DEMO-Rxx`、`review_note_DEMO-Rxx`。只有初次顯示時由已儲存案件填入，不在每次 rerun 覆寫。
- 「儲存本項確認」用 form_submit_button，一次提交結果及備註；回呼在畫面重繪前呼叫 save_review。兩頁統計、清單、草稿未檢視清單及最終確認都讀取同一 reviews。
- 輸入但尚未儲存不影響案件統計。按儲存後才計入已檢視。備註或處理結果改變時，revision 增加、撤回原確認，草稿變成過期；相同內容重複儲存不增加 revision。
- 分析只保留 stage（已完成階段數）、analysis_started、error；刪除獨立 analyzed。完成狀態由 stage 與 error 推導。
- analysis_snapshot 一次產生總進度、各階段狀態及完成判斷。每次只推進一個階段後整頁 rerun，五張卡片以一個完整區塊輸出，不反覆修改子容器。
- 草稿編輯器同樣避免每次重繪覆寫；重新產生草稿時才重建輸入值。

## 測試

實際執行 `.venv\Scripts\python.exe -m unittest discover -v`，共 10 項，全數通過，最後一次耗時 8.686 秒。

保留原本 5 項測試情境，更新為實際按儲存及由 stage 判斷分析完成。新增 test_state_sync.py 的 5 項測試：

1. 分析每階段快照、完成總進度、中斷與重試。
2. 重複儲存不重複修改版本、只改備註使草稿過期、案件工作階段互不影響。
3. 完成後實際渲染五個「已完成」、100% 與 5／5，並驗證重設為尚未分析。
4. 未儲存不計入已檢視，提交後立即顯示 1／8 及尚未確認 7 項。
5. 完整驗收：R01 儲存、清單更新、R02 往返、完成八項、兩頁 8／8、逐項結果與備註保留、最終確認、改回一項未確認後兩頁 7／8、確認撤回及草稿過期，重新產生後列出正確未決事項。

## 不變範圍及已知限制

- ui.css 與 demo_data.py 的 SHA256 與本輪修改前完全相同，未改配色、案件、DEMO-R01–R08 或歷史案例內容。
- 目前自動測試未發現仍存在的狀態同步錯誤；本輪互動驗證使用 Streamlit AppTest，未進行瀏覽器自動點擊驗證。
- 資料仍僅保存於目前 Streamlit 工作階段。重設、重啟服務或建立新工作階段會清除；不提供跨工作階段永久保存。
