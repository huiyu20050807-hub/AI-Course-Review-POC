# 第一版 POC 驗證紀錄

## 狀態同步修正複驗（2026-09-10）

原有 5 項情境更新為按鈕提交，另新增 5 項狀態測試。10 項全數通過（8.686 秒），包含完整 12 步驗收與每階段顯示、跨頁統計、備註保存、草稿過期及確認失效。原因與測試界線詳見「狀態同步修正紀錄.md」。此紀錄優先於下方舊版自動保存說明。

## 1920×1080 投影展示複驗（2026-09-10）

- 主要內文調整為 21px，頁面／區塊標題提升層級；結果卡片數字 56px，DEMO-R07 的 36、42、6 小時採 58px 呈現。
- 首頁數值摘要收合，優先說明 AI 用途；結果原文與規定可展開，歷史卡片只顯示課程名稱、相似原因及重要差異。
- 新增只讀的「AI 初審摘要」，呈現原始 AI 結果 4／2／2、主要問題與建議，不覆蓋人工判斷或完整草稿。
- 所有既有 5 項測試通過，耗時 6.862 秒；demo_data.py、review_logic.py、test_poc.py 的 SHA256 與修改前一致。
- 本次僅調整 app.py 呈現與 ui.css。未執行實際投影設備或瀏覽器截圖驗收。

## UI/UX 展示優化複驗（2026-09-10）

- 僅修改 app.py 的呈現與版面，新增 ui.css；八項規則、虛構案件與案例資料、案件邏輯均未改動。
- demo_data.py、review_logic.py、test_poc.py 的 SHA256 與優化前完全相同。導覽與編輯回呼亦經語法樹比對確認一致。
- 重新執行原有 5 項測試，全數通過，耗時 5.919 秒。
- 全站固定 POC 聲明保留；加大按鈕與閱讀字級，案件狀態獨立呈現，結果以四個編號區塊分隔；初審意見編輯區增加高度及行距。
- 本次未執行瀏覽器截圖或實際投影設備視覺驗收。

測試日期：2026-09-10（臺北時間）

## 執行環境

- Windows、Python 3.14.6、Streamlit 1.63.0。
- 套件安裝在專案 `.venv`，完整版本見 `requirements-lock.txt`。
- 服務僅監聽 `127.0.0.1:8501`；已關閉使用統計。

## 功能測試

使用 Streamlit 官方 AppTest 執行畫面元件互動，以及 Python unittest 驗證案件規則，共 **5 項測試全數通過**。最後一次執行耗時 6.670 秒，沒有應用程式 exception。

1. 虛構資料一致性：DEMO-R01–R08、4／2／2 狀態分布、三筆歷史案例；所有證據摘錄確實存在於對應計畫書章節。
2. 確認邏輯：未檢視完不能確認；過期草稿不能確認；須主動勾選聲明；確認後修改內容會撤回確認。
3. 完整操作：首頁載入與預覽、五階段完成、結果、八項人工選擇及備註、三案例切換、產生草稿、人工修訂、儲存與確認、返回後資料保留、草稿過期、重新產生保留舊版、取消及確認重設。
4. 篩選與未檢視保護：狀態篩選、只顯示尚未確認、篩選無結果、草稿列出未檢視項目、阻擋最終確認。
5. 分析失敗重試：測試注入失敗狀態，確認可顯示失敗並重新完成五階段。

測試命令：`.venv\Scripts\python.exe -m unittest -v test_poc.py`。

## 服務檢查

- 已實際啟動本機服務，網址 `http://127.0.0.1:8501`。
- 首頁 HTTP 回應 200，`/_stcore/health` 回應 `ok`。
- 最後修改後已重新啟動服務，保留背景執行供展示。
- 功能互動以 AppTest 驗證；未執行瀏覽器截圖或實際投影設備的視覺驗收。

## 展示邊界

- 分析進度、讀取結果、歷史比對與初審草稿是固定資料與範本的模擬，不使用 LLM。
- 人工操作、文字編輯、狀態檢查與版本保存是真實功能，但保存在目前工作階段，無跨工作階段資料庫。
- 確認初審意見不是核定課程。下載檔案包含虛構資料聲明及當下確認狀態。
